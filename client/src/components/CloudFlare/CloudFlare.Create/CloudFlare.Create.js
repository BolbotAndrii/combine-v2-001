import React from 'react';
import { CloudFlareCreateForm } from "./CloudFlare.Create.Form/CloudFlare.Create.Form";

export const CloudFlareCreate = () => {
    return (
        <div>
            <CloudFlareCreateForm />
        </div>
    )
}
